apiVersion: v1
kind: Service
metadata:
  name: swe645-service
spec:
  selector:
    app: swe645-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30007
  type: NodePort